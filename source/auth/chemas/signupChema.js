/*
    {
        userName: String!, (number || letter || . || _ || 30)
        mailOrPhoneNumber: String!, (mail || phoneNumber)
        fullName: String!,  (letter || 30)
        password: MD5!  (md5)
    }
*/